namespace ArrowIO {
    export async function start() {
        // // Load resource.
        // await RES.loadConfig("resource/default.res.json", "resource/");
        // await RES.getResAsync("Assets/obstacle3.prefab.json");
        // await RES.getResAsync("Assets/pp1.prefab.json");
        // await RES.getResAsync("Assets/Exp.prefab.json");

        // // Create camera.
        // egret3d.Camera.main.gameObject.addComponent(behaviors.RotateComponent);
        // // egret3d.Camera.main.cullingMask = paper.CullingMask.Everything;

        // {
        //     let lightObject = paper.GameObject.find("Directional light");
        //     if (!lightObject) {
        //         lightObject = new paper.GameObject("Directional light");
        //     }
        //     let light = lightObject.addComponent(egret3d.DirectionalLight);
        //     light.color.set(1.0, 1.0, 1.0, 1.0);
        //     light.intensity = 0.75;
        //     light.shadowCameraFar = 1000;
        //     light.shadowCameraNear = 1;
        //     light.shadowCameraSize = 30;
        // }

        // // Create scene.
        // paper.Prefab.create("Assets/obstacle3.prefab.json");
        // egret3d.combine(paper.Scene.activeScene.gameObjects);
        // const container = paper.GameObject.create();

        // for (let i = 0; i < 9; ++i) {
        //     const player = paper.Prefab.create("Assets/pp1.prefab.json");
        //     player.transform.setLocalPosition(Math.random() * 40.0 - 20.0, 0.0, Math.random() * 40.0 - 20.0);
        //     player.parent = container;
        // }

        // for (let i = 0; i < 140; ++i) {
        //     const exp = paper.Prefab.create("Assets/Exp.prefab.json");
        //     exp.transform.setLocalPosition(Math.random() * 40.0 - 20.0, 0.0, Math.random() * 40.0 - 20.0);
        //     exp.parent = container;
        // }
    }
}